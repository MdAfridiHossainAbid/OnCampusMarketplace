// The list of campus locations buyers are allowed to choose for pickup.
// Edit this array to add/remove valid locations — everything else reads from here.
const PICKUP_LOCATIONS = [
  'Main Gate',
  'Library',
  'Cafeteria',
  'Academic Building',
];

module.exports = { PICKUP_LOCATIONS };